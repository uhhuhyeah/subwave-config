# Copy to .env and fill in. .env is gitignored (it holds the admin password).

# --- config sync (pull.sh / push.sh): admin API over HTTP ---
SUBWAVE_URL=http://192.168.1.18:7700
SUBWAVE_ADMIN_USER=david
SUBWAVE_ADMIN_PASS=changeme

# --- avatar sync (avatars-pull.sh / avatars-push.sh): file-copy via PVE + pct exec ---
# Defaults below are baked into the scripts; only set these to override.
# PVE_SSH=root@100.110.0.9
# SUBWAVE_CTID=107
# SUBWAVE_AVATAR_DIR=/opt/subwave/state/persona-avatars
